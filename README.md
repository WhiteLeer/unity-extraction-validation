# unity-extraction-validation

Unity 提取验收场：用于导入、绑定、Timeline 重建与资源恢复验证，不等同于底层提取工具仓库。

## 用途

- 验证提取后的资源能否在 Unity 内正确导入与显示
- 记录绑定、重建、验收所需的工程侧脚本与场景
- 为资源提取流程提供最终效果对照

## 与其他仓库的关系

- `sr-assetstudio-toolchain`
  - 负责底层提取、解析与批处理工具链
- `unity-extraction-validation`
  - 负责导入后的 Unity 侧验收与恢复
